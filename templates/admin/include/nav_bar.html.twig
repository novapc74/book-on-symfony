<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="{{ path('app_book') }}">Books</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
            <a class="nav-link text-warning" href="{{ path('admin_author') }}">Authors</a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-warning" href="{{ path('admin_category') }}">Categories</a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-warning" href="{{ path('admin_book') }}">Books</a>
        </li>
      </ul>
        <ul class="navbar-nav mr-right mx-3">
        {% if app.user %}
            <li class="nav-item">
                <a class="nav-link" href="#">{{ app.user.email }}</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_logout') }}">logout</a>
            </li>
        {% else %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_login') }}">Login</a>
            </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>